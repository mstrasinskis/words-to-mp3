# tsv-to-mp3

Converts TSV vocabulary rows formatted for Anki import into MP3 audio files using OpenAI TTS.

Set `OPENAI_API_KEY` before running. You can also set `START_INDEX` to prefix output files with a zero-padded sequence such as `001.`, `099.`, or `240.`.
